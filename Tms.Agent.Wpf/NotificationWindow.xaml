<Window x:Class="Tms.Agent.Wpf.NotificationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Ειδοποίηση" 
        Height="140" Width="330"
        WindowStyle="None" 
        AllowsTransparency="True" 
        Background="Transparent"
        Topmost="True" 
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        FontFamily="Segoe UI, Roboto, Helvetica, Arial, sans-serif">

    <Border CornerRadius="8" Background="#16161F" BorderBrush="#2B2B3C" BorderThickness="1">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="6" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- Accent Indicator Bar on the Left -->
            <Border x:Name="AccentBar" Grid.Column="0" CornerRadius="8,0,0,8" Background="#6366F1" />

            <Grid Grid.Column="1" Margin="12,10,12,10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <!-- Header & Close Icon -->
                <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <StackPanel Orientation="Horizontal" Grid.Column="0">
                        <TextBlock x:Name="IconText" Text="📢" FontSize="14" Margin="0,0,6,0" VerticalAlignment="Center"/>
                        <TextBlock x:Name="HeaderText" Text="Νέα Ανακοίνωση" FontSize="11" FontWeight="Bold" Foreground="#A0A0B0" VerticalAlignment="Center"/>
                    </StackPanel>
                    <Button Grid.Column="1" Content="×" FontSize="18" Foreground="#A0A0B0" Background="Transparent" BorderThickness="0" Cursor="Hand" Click="CloseButton_Click" VerticalAlignment="Center" Width="20" Height="20">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Foreground" Value="#FFFFFF" />
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </Grid>

                <!-- Title & Content -->
                <StackPanel Grid.Row="1" Margin="0,6,0,6">
                    <TextBlock x:Name="TitleText" Text="Τίτλος Ανακοίνωσης" FontSize="13" FontWeight="Bold" Foreground="#FFFFFF" Margin="0,0,0,2" TextTrimming="CharacterEllipsis"/>
                    <TextBlock x:Name="MessageText" Text="Περιεχόμενο ανακοίνωσης..." FontSize="11" Foreground="#A5B4FC" TextWrapping="Wrap" Height="36" TextTrimming="CharacterEllipsis"/>
                </StackPanel>

                <!-- Actions -->
                <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,2,0,0">
                    <Button Content="Κλείσιμο" Width="70" Height="24" Margin="0,0,8,0" Click="CloseButton_Click" Cursor="Hand">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="Foreground" Value="#A0A0B0"/>
                                <Setter Property="BorderBrush" Value="#2B2B3C"/>
                                <Setter Property="BorderThickness" Value="1"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="4">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#2B2B3C"/>
                                        <Setter Property="Foreground" Value="#FFFFFF"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>

                    <Button x:Name="ViewButton" Content="Προβολή" Width="70" Height="24" Click="ViewButton_Click" Cursor="Hand">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Background" Value="#6366F1"/>
                                <Setter Property="Foreground" Value="#FFFFFF"/>
                                <Setter Property="BorderThickness" Value="0"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}" CornerRadius="4">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#4F46E5"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</Window>
